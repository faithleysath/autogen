Implement only the current stage, not the whole roadmap.
- Keep changes cohesive and biased toward the smallest diff that satisfies the stage exit criteria.
- When a service or watcher is needed for validation, prefer creating a background task and then inspect its output.
- Leave the workspace in a state that the stage gate can verify without guessing hidden assumptions.
